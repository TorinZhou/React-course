import MealsSummary from "./MealsSummary";
import AvailableMeals from "./AvailableMeals";

const Meals = (props) => {
  return (
    <main>
      <MealsSummary />
      <AvailableMeals />
    </main>
  );
};

export default Meals;
